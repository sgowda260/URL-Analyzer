# URL Analyzer

URL Analyzer is a simple FastAPI dashboard for reviewing website safety. It checks links with Google Safe Browsing, calculates a basic health score, identifies URL warnings, and provides a short website summary using available page details or Gemini.

The project uses Redis for caching and rate limiting and keeps the Python application in a single file.
